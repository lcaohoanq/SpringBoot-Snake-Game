spring.application.name=Spring-Snake-Game
spring.output.ansi.enabled=ALWAYS

server.port=8081

# API
v1API=/api/v1

# MySQL database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3308/mysql_starter}
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD:Luucaohoang1604^^}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
jwt.secret=your_jwt_secret_key
jwt.access.expiration=600000
jwt.refresh.expiration=1209600000

logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG
